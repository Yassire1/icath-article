% NOTE: Results sections below contain [TBD] placeholders that MUST be filled
% with real experimental results before submission.

\subsection{Zero-Shot Performance}

Table~\ref{tab:zero_shot_mae} presents the zero-shot MAE results for all three TSFMs and the
supervised PatchTST baseline across the three datasets. [TBD: Summarize key finding after
experiments are complete.]

% Table is defined in paper/tables/table3_zero_shot.tex
\input{tables/table3_zero_shot}

[TBD: Discuss which model performed best/worst and on which dataset. Note whether
Chronos or Lag-Llama (both univariate) degraded less or more than MOMENT (multivariate)
on the high-channel-count Wind SCADA data.]

\subsection{Few-Shot Adaptation}

Table~\ref{tab:few_shot} is reserved for exploratory adapter-based fine-tuning results with 1\% of available labels. It should only be populated once the open-source wrappers perform real parameter updates for MOMENT and Lag-Llama. [TBD: Summarize the gap between zero-shot and few-shot performance only if that implementation is completed.]

% Table is defined in paper/tables/table4_few_shot.tex
\input{tables/table4_few_shot}

[TBD: Discuss whether any validated few-shot gains are consistent across datasets or vary. Omit this subsection entirely if the adapter implementation remains incomplete.]

\subsection{Cross-Condition Transfer on C-MAPSS}

Table~\ref{tab:rul} reports MAE for all models when evaluated on C-MAPSS subsets FD001--FD004,
where FD002--FD004 represent different fan operating conditions and fault modes unseen during
training. [TBD: Summarize whether zero-shot TSFMs generalize better or worse than
PatchTST trained only on FD001.]

% Table is defined in paper/tables/table5_rul.tex
\input{tables/table5_rul}

[TBD: Discuss condition-specific failure modes visible in the results.]
